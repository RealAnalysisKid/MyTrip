<div class="row">
  <div class="col-md-12">
      <h2>城市</h2>
        <small style="text-left">
          世界如此迷人 那么多地方想去瞧瞧
        </small>

      <table class="table table-striped">
        <tbody>
          <tr>
            <% @cities.each do |city| %>
             <td><%= link_to(city.title, city_path(city)) %></td>
             <td><%= city.created_at.to_s(:short) %></td>
            <% end %>
          </tr>
        </tbody>
      </table>
  </div>
</div>
