<h2>Add a City</h2>

<%= simple_form_for [:admin,@city] do |f| %>

<%= f.input :title %>
<%= f.input :description %>
<%= f.input :is_hidden %>

<%= f.submit "Submit", data: { disable_with: "Submitting..." } %>

<% end %>
